<?xml version="1.0" encoding="utf-8"?>
<!-- Shared by all 12 CompanyOverviewActivity tiles (finance + HR grids) — mirrors the dashboard's
     stat-card pattern (icon + big bold number + small muted label) in a bg_card tile. The icon is
     set per-instance from Java (CompanyOverviewActivity.bindStat) since this template is reused
     via <include> for stats with different meanings. -->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:background="@drawable/bg_card"
    android:orientation="vertical"
    android:padding="@dimen/card_padding_compact">

    <ImageView
        android:id="@+id/statIcon"
        android:layout_width="@dimen/icon_size_small"
        android:layout_height="@dimen/icon_size_small"
        android:importantForAccessibility="no"
        app:tint="@color/text_muted" />

    <TextView
        android:id="@+id/statValue"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="@dimen/space_8"
        android:text="@string/stat_placeholder"
        android:textColor="@color/brand_primary"
        android:textSize="22sp"
        android:textStyle="bold" />

    <TextView
        android:id="@+id/statLabel"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:textAppearance="@style/TextAppearance.Zuhoo.BodySmall" />

</LinearLayout>
