<?xml version="1.0" encoding="utf-8"?>
<!-- Staff mode: four tabs. No catalog or billing — those are client-facing, and the staff
     equivalents are web-only by design. Ids are shared with the client menu so one listener
     handles both. -->
<menu xmlns:android="http://schemas.android.com/apk/res/android">

    <item
        android:id="@+id/nav_home"
        android:icon="@drawable/ic_home"
        android:title="@string/nav_home" />

    <item
        android:id="@+id/nav_requests"
        android:icon="@drawable/ic_requests"
        android:title="@string/nav_requests" />

    <item
        android:id="@+id/nav_support"
        android:icon="@drawable/ic_support"
        android:title="@string/nav_support" />

    <item
        android:id="@+id/nav_account"
        android:icon="@drawable/ic_account"
        android:title="@string/nav_account" />

</menu>
